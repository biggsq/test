<aura:component >
    <aura:attribute name="message" type="String" default="no message"/>
    <p>{!'Hello! ' + v.message}</p>
    <p>{!v.message}</p>
    <div>
        <lightning:button label="You look nice today." value="999"
            onclick="{!c.handleClick}"/>
 
        <lightning:button label="Today is going to be a great day!"
            onclick="{!c.handleClick}"/>
            <button onclick="{!c.handleClick9}">123</button>
    </div>
</aura:component>